name: CI

on:
  push:
    branches: [main]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo code
        uses: actions/checkout@v7

      - name: List files
        run: ls

      - name: Say hello
        run: echo "hello from CI"
